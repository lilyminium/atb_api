name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # package
  - pydantic  # possibly overkill
  - pyyaml
  - numpy
  - elementable

  - rdkit
  - mdanalysis

    # Pip-only installs
  #- pip:
  #  - codecov

