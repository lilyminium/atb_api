name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # package
  - pydantic  # possibly overkill
  - pyyaml
  - numpy

    # Pip-only installs
  #- pip:
  #  - codecov

